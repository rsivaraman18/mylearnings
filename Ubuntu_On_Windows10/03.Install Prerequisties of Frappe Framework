Pre_Requisites

  1. Python 3.6+
  2. Github         --> Cloning from main Repository
  2. Node.js 14+
  3. Redis 5
  4. MariaDB 10.3.x  or Postgres 9.5.x
  5.yarn 1.12+
  6.pip 20+
  7.wkhtmltopdf (version 0.12.5 with)
  8.cron
  9.NGINX

******************************************************************
Installation Steps:

1. Install Python
   Cmd : sudo apt install python3
   Out : Successfully Installed.

2. Install Github.
   Cmd : Cmd : sudo apt-get install git
   Out : Successfully Installed.

   Cmd: git --version
   Out:git version 2.34.1

3. Install python3-dev.
   Cmd : sudo apt-get install python3-dev
   Out : Successfully Installed.

4. Install setup tools & pip
   Cmd : sudo apt-get install python3-setuptools python3-pip
   Out : Successfully Installed.

5. Install Virtual Environment
   Cmd : sudo apt-get install virtualenv
   Out : Successfully Installed.  
