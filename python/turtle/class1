# Turtle is a builtin function in Python.
# So we can use it using the command Import turtle.
# it is graphical represantational of a structure.

Problem Cases:
1.To Draw a circle with 50 radius.
2.To Draw a circle with user input data
3.To draw a Rectangle with width = 150 & height = 80.
4:To draw a square with radius 100 using for loop 




#Case 1: To Draw a circle with 50 radius.
def circle1( ):
  import turtle       
  import time
  
  
  #circle_rad = int(input("enter your radius : " ))  #Enter radius
  circle_rad = 50

  alex = turtle.Turtle()  # create a turtle named alex
  alex.shape("turtle")    # alex looks like a turtle
  alex.color('red')       # alex has a color
  alex.circle(circle_rad)
  time.sleep(5)           # hold for certain time


#Case 2:To Draw a circle with user input data
def circle2( ):
  import turtle       
  import time  
  
  circle_rad   = int(input("enter your radius : " ))  
  circle_color = input("color of circle : " )
  
  alex = turtle.Turtle()        # create a turtle named alex
  alex.shape("turtle")          # alex looks like a turtle
  alex.color(circle_color)      # alex has a color
  alex.circle(circle_rad)
  time.sleep(5) 

#Case 3: To draw a Rectangle with width = 150 & height = 80.

def rect1( ):
    import turtle
    import time
    
    wn = turtle.Screen()    # creates a graphics window
    wn.setup(400,400) 
             
    rectangle_width=150     #width = 150
    rectangle_height=80     #height = 80

    alex = turtle.Turtle()  
    alex.shape("turtle") 
        
    alex.forward(rectangle_width)
    alex.right(90)
    alex.forward(rectangle_height)
    alex.right(90)
    alex.forward(rectangle_width)
    alex.right(90)
    alex.forward(rectangle_height)
    time.sleep(5) 

rect1( )

case 4:To draw a square with radius 100 using for loop 
def square1( ):
  import turtle       
  
  wn = turtle.Screen()    # creates a graphics window
  wn.setup(400,400)
  side = 100
  alex = turtle.Turtle()
  alex.shape("turtle")
  
  for i in range(1,5 ):
      alex.forward(side)
      alex.right(90)
    

square1( )

case 5: :To draw a Triangle with radius 100     
def triangle( ):
    wn = turtle.Screen()    # creates a graphics window
    wn.setup(400,400)
    side = 100
    alex = turtle.Turtle()
    alex.shape("turtle")

    alex.right(180)
    alex.forward(side)
    alex.right(120)
    alex.forward(side)
    alex.right(120)
    alex.forward(side)
